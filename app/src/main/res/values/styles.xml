<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark theme for Settings Dialog -->
    <style name="SettingsDialogTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:colorBackground">@color/bg_card</item>
        <item name="android:colorForeground">#FFFFFF</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:windowBackground">@color/bg_card</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>
</resources>
